<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KnigolyubApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/shimmerColor</item>
        <item name="cardBackgroundColor">@color/black</item>
        <item name="background">@color/shimmerColor</item>
        <item name="progressColor">@color/red</item>
        <item name="totalColor">@color/shimmerColor</item>
        <item name="tabIndicatorColor">@color/white</item>
        <item name="progressTextColor">@color/shimmerColor</item>
        <item name="android:fontFamily">@font/comic_sans</item>
        <item name="android:textColor">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/red</item>
        <item name="colorSecondaryVariant">@color/red</item>
        <item name="colorOnSecondary">@color/shimmerColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>